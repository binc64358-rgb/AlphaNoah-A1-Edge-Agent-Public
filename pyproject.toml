[project]
name = "alphanoah-a1-edge-agent"
version = "0.1.0"
description = "A local, auditable runtime foundation for industrial field agents"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
alphanoah-demo = "alphanoah_a1.demo:main"

[tool.setuptools.packages.find]
where = ["src"]
include = ["alphanoah_a1*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
