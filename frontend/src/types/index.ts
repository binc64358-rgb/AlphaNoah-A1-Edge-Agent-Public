/**
 * Compatibility type export for visual primitives outside the Runtime
 * feature. Runtime status and View Model types live in features/runtime.
 */
export type {
  PresentationSeverity as Severity,
} from "../features/runtime";
