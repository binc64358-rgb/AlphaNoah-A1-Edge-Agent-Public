# Provider examples. Never put real credentials in a release archive.
# Ollama: ALPHANOAH_PROVIDER=ollama, base URL http://127.0.0.1:11434
# vLLM uses ALPHANOAH_PROVIDER=openai_compatible with a /v1 base URL.
# Remote APIs also use openai_compatible and require explicit egress consent.
